openapi: 3.1.0
info:
  title: URL Shortener API
  version: 1.0.0
  description: API runtime contract for owned link management, redirects, click totals, system information, and health.
servers:
  - url: /
    description: Current API origin
tags:
  - name: Links
  - name: Analytics
  - name: System
  - name: Health
paths:
  /api/v1/links:
    post:
      tags: [Links]
      summary: Create a short link
      operationId: createLink
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateLinkRequest'
      responses:
        '201':
          description: Link created
          headers:
            Location:
              description: Public short URL
              schema:
                type: string
                format: uri
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LinkResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '409':
          $ref: '#/components/responses/Conflict'
        '429':
          $ref: '#/components/responses/TooManyRequests'
        '503':
          $ref: '#/components/responses/ServiceUnavailable'
  /api/v1/links/{code}:
    parameters:
      - $ref: '#/components/parameters/Code'
    get:
      tags: [Links]
      summary: Get a link owned by the caller
      operationId: getLink
      security:
        - OwnerToken: []
      responses:
        '200':
          description: Current link state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LinkResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
    patch:
      tags: [Links]
      summary: Update destination, status, or expiration using optimistic concurrency
      operationId: updateLink
      security:
        - OwnerToken: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateLinkRequest'
      responses:
        '200':
          description: Updated link
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LinkResponse'
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
    delete:
      tags: [Links]
      summary: Permanently delete a link and its analytics
      description: Requires the owner token and current version to prevent deleting a concurrently changed link.
      operationId: deleteLink
      security:
        - OwnerToken: []
      parameters:
        - name: expectedVersion
          in: query
          required: true
          schema:
            type: integer
            format: int64
            minimum: 0
      responses:
        '204':
          description: Link and associated analytics deleted
        '400':
          $ref: '#/components/responses/BadRequest'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '409':
          $ref: '#/components/responses/Conflict'
  /api/v1/links/{code}/analytics:
    get:
      tags: [Analytics]
      summary: Get the total persisted clicks for a short link
      description: Requires the capability token returned once when the link is created.
      operationId: getLinkAnalytics
      security:
        - OwnerToken: []
      parameters:
        - $ref: '#/components/parameters/Code'
      responses:
        '200':
          description: Click total
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnalyticsResponse'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '410':
          $ref: '#/components/responses/Gone'
  /api/v1/system/info:
    get:
      tags: [System]
      summary: Get build and runtime information
      operationId: getSystemInfo
      responses:
        '200':
          description: System information
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemInfo'
  /actuator/health:
    get:
      tags: [Health]
      summary: Get aggregate application health
      operationId: getHealth
      responses:
        '200':
          $ref: '#/components/responses/HealthResponse'
        '503':
          $ref: '#/components/responses/HealthResponse'
  /actuator/health/liveness:
    get:
      tags: [Health]
      summary: Get liveness health
      operationId: getLiveness
      responses:
        '200':
          $ref: '#/components/responses/HealthResponse'
        '503':
          $ref: '#/components/responses/HealthResponse'
  /actuator/health/readiness:
    get:
      tags: [Health]
      summary: Get readiness health
      operationId: getReadiness
      responses:
        '200':
          $ref: '#/components/responses/HealthResponse'
        '503':
          $ref: '#/components/responses/HealthResponse'
  /{code}:
    get:
      tags: [Links]
      summary: Resolve a short link
      operationId: resolveLink
      parameters:
        - $ref: '#/components/parameters/Code'
      responses:
        '302':
          description: Redirect to the destination URL
          headers:
            Location:
              required: true
              schema:
                type: string
                format: uri
            Cache-Control:
              schema:
                type: string
              example: no-store
            Referrer-Policy:
              schema:
                type: string
              example: no-referrer
        '404':
          $ref: '#/components/responses/NotFound'
        '410':
          $ref: '#/components/responses/Gone'
components:
  securitySchemes:
    OwnerToken:
      type: apiKey
      in: header
      name: X-Link-Owner-Token
  parameters:
    Code:
      name: code
      in: path
      required: true
      schema:
        type: string
        pattern: '^[A-Za-z0-9_-]{4,32}$'
  responses:
    BadRequest:
      description: Invalid request
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    Conflict:
      description: Short code conflict
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    Forbidden:
      description: Missing or invalid owner token
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    TooManyRequests:
      description: Link creation rate limit exceeded
      headers:
        Retry-After:
          schema:
            type: integer
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    NotFound:
      description: Short link not found
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    Gone:
      description: Short link expired or disabled
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    ServiceUnavailable:
      description: Short-code allocation unavailable
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
    HealthResponse:
      description: Spring Boot health response
      content:
        application/vnd.spring-boot.actuator.v3+json:
          schema:
            $ref: '#/components/schemas/Health'
  schemas:
    CreateLinkRequest:
      type: object
      required: [destinationUrl]
      properties:
        destinationUrl:
          type: string
          format: uri
          maxLength: 2048
          description: Absolute HTTP or HTTPS URL without credentials.
          example: https://example.com/docs
        customAlias:
          type: [string, 'null']
          pattern: '^[A-Za-z0-9_-]{4,32}$'
          example: docs-2026
        expiresAt:
          type: [string, 'null']
          format: date-time
          description: Must be in the future when supplied.
    LinkResponse:
      type: object
      required: [id, code, shortUrl, destinationUrl, status, createdAt, version]
      properties:
        id:
          type: string
          format: uuid
        code:
          type: string
        shortUrl:
          type: string
          format: uri
        destinationUrl:
          type: string
          format: uri
        status:
          type: string
          enum: [ACTIVE, DISABLED]
        expiresAt:
          type: [string, 'null']
          format: date-time
        createdAt:
          type: string
          format: date-time
        version:
          type: integer
          format: int64
          minimum: 0
        ownerToken:
          type: string
          description: Returned only during creation. Store it securely; it cannot be recovered.
    UpdateLinkRequest:
      type: object
      required: [expectedVersion]
      properties:
        expectedVersion:
          type: integer
          format: int64
          minimum: 0
        destinationUrl:
          type: [string, 'null']
          format: uri
          maxLength: 2048
        status:
          type: [string, 'null']
          enum: [ACTIVE, DISABLED, null]
        expiresAt:
          type: [string, 'null']
          format: date-time
    AnalyticsResponse:
      type: object
      required: [code, totalClicks]
      properties:
        code:
          type: string
        totalClicks:
          type: integer
          format: int64
          minimum: 0
    SystemInfo:
      type: object
      required: [service, version, runtimeRole]
      properties:
        service:
          type: string
          example: url-shortener
        version:
          type: string
        runtimeRole:
          type: string
          enum: [API, WORKER]
    Health:
      type: object
      required: [status]
      properties:
        status:
          type: string
          example: UP
      additionalProperties: true
    Problem:
      type: object
      required: [type, title, status, detail]
      properties:
        type:
          type: string
          format: uri
        title:
          type: string
        status:
          type: integer
          minimum: 400
          maximum: 599
        detail:
          type: string
        instance:
          type: string
          format: uri
        errors:
          type: array
          items:
            type: string
      additionalProperties: true
